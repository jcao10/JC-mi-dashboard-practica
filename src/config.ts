export const config = {
  apiUrl: import.meta.env.VITE_API_URL || 'http://localhost:3000/api',
};

// User Story:
// Como desarrollador, quiero tener un archivo de configuración centralizado
// para manejar las variables de entorno y configuraciones de la aplicación,
// para mantener un código más mantenible y seguro. 